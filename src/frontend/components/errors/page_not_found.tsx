import React from 'react';
import { withStyle } from './../aux_component';

const PageNotFound = () => {
    return <p>Page not found</p>;
};
export default withStyle(PageNotFound, 'page_not_found');
